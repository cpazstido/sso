server:
  port: 8080
  servlet:
    context-path: /auth
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/oh_coupon?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  redis:
    host: 127.0.0.1